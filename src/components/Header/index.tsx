import { Typography } from "@progress/kendo-react-common";

export const Header = () => {
    return(
      <Typography.h1 textAlign="center">
        Feed React and Morty
      </Typography.h1>
    );
};